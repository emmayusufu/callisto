{
  "name": "@callisto/server",
  "version": "1.0.0",
  "private": true,
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "dev": "nodemon --watch 'src/**/*.ts' --exec 'pnpm build && node dist/index.js'",
    "build": "tsc",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@callisto/shared": "workspace:*",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "mediasoup": "^3.12.16",
    "socket.io": "^4.7.2"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^4.17.20",
    "@types/node": "^20",
    "nodemon": "^3.0.1",
    "typescript": "^5.3.3"
  }
}
